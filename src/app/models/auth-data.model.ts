export interface loginAuthData {
    email: string;
    password: string;
    role: string;
}

export interface signupAuthData {
    first_name: string;
    last_name: string;
    email: string;
    password : string;
    mobile: number;
    role: string;
}